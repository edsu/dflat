from setuptools import setup, find_packages

setup(
    name = 'dflat',
    version = '0.1',
    description = "",
    author = "Ed Summers",
    author_email = "ehs@pobox.com",
    url = "",
    download_url = "",
    packages = find_packages(),
    test_suite = 'test',
)
